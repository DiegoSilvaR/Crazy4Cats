<%= form_with(model: like, class: "form") do |form| %>
  <% if like.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(like.errors.count, "error") %> impidieron que este like se guardara:</h2>

      <ul>
        <% like.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :user_id, "Usuario", class: "form-label", style: "display: block" %>
    <%= form.text_field :user_id, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :post_id, "Publicación", class: "form-label", style: "display: block" %>
    <%= form.text_field :post_id, class: "form-control" %>
  </div>

  <div class="text-center">
    <%= form.submit "Guardar", class: "btn btn-primary" %>
  </div>
<% end %>
